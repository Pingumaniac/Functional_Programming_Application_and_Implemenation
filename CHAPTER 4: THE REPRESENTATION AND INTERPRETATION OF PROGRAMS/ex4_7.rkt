#lang racket

#|
The result of (VALOF e) = value of the variable whose name is computed as the value of e.

Allows to import names into internal contexts
-> not a desirable feature as it is very complex to use.
|#